{
  "extends": "./tsconfig.json",
  "exclude": [
    "node_modules",
    "dist",
    "test",
    // Tests now live in `test/`, but keep these so a stray or in-progress
    // test left under src/ can never break a production build.
    "src/**/*.test.ts",
    "src/**/__tests__/**"
  ]
}
